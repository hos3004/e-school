<?php

declare(strict_types=1);

/**
 * سجل الموديولات.
 *
 * الترتيب = ترتيب التحميل، ويعكس اتجاه الاعتماد في docs/19-agent-dependency-graph.md
 * الموديول الأعلى في القائمة لا يجوز أن يعتمد على موديول أسفله.
 *
 * إطفاء موديول هنا يمنع تحميل مسارات وهجرات وترجمات ومزوّد خدمة الموديول بالكامل.
 */
return [

    'enabled' => [

        // ── الطبقة 0: النواة ─────────────────────────────────────────────
        'Organization' => true,   // المؤسسة، الإعدادات، التقويم الأكاديمي، الإجازات
        'Identity' => true,       // الحسابات، الدخول، التحقق، الأجهزة
        'AccessControl' => true,  // الأدوار والصلاحيات
        'Audit' => true,          // سجل التدقيق
        'Integrations' => true,   // مزوّدو الخدمات الخارجية
        'Notifications' => true,  // محرّك الإشعارات والقنوات

        // ── الطبقة 1: الأشخاص ────────────────────────────────────────────
        'Students' => true,
        'Guardians' => true,      // أولياء الأمور — أكثر من 50% من الطلاب قُصّر
        'Staff' => true,          // المعلمون والموظفون والعقود والإتاحة

        // ── الطبقة 2: البنية الأكاديمية ──────────────────────────────────
        'Academics' => true,      // البرامج والمستويات والمواد والمناهج
        'Groups' => true,         // المجموعات الدراسية (حتى 25 طالبًا)
        'Enrollments' => true,    // القيد ودورة حياته
        'Content' => true,        // مكتبة المواد التعليمية

        // ── الطبقة 3: التشغيل ────────────────────────────────────────────
        'Scheduling' => true,     // الجدولة والتعارضات والتأجيل وحصص التلافي
        'Sessions' => true,       // الحصة — أهم كيان في المنصة
        'Attendance' => true,     // الحضور والغياب
        'VirtualClassroom' => true, // الفصل المباشر (BigBlueButton)
        'Recordings' => true,     // التسجيلات والاحتفاظ والأرشفة

        // ── الطبقة 4: التعلّم ────────────────────────────────────────────
        'Assignments' => true,    // الواجبات
        'Assessments' => true,    // الاختبارات والدرجات + اختبار فك التجميد
        'AcademicReports' => true, // تقرير الحصة والتقرير الشهري
        'Certificates' => true,   // الشهادات والبادچات

        // ── الطبقة 5: الانضباط والتواصل ──────────────────────────────────
        'Discipline' => true,     // الإنذارات والتجميد وإعادة التفعيل
        'Messaging' => true,      // مراسلات المنصة + حائط الصف

        // ── الطبقة 6: المال ──────────────────────────────────────────────
        // ADR-017: احتساب أجر المعلم داخل المرحلة الأولى — الدفتر يعمل.
        'Payroll' => (bool) env('MODULE_PAYROLL_ENABLED', true),
        'Billing' => false,       // فوترة الطلاب — مؤجّلة للمرحلة الثانية

        // ── الطبقة 7: القراءة ────────────────────────────────────────────
        'Reporting' => true,      // لوحات المعلومات والتقارير والتصدير
    ],

    /**
     * حدود صارمة: الموديول المذكور هنا لا يجوز لأي موديول آخر أن يستورد
     * كياناته مباشرة. تُنفَّذ آليًا في tests/Architecture.
     */
    'sealed_domains' => [
        'Payroll',
        'Billing',
        'Audit',
        'Identity',
    ],
];
