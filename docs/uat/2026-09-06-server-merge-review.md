# مراجعة دمج تحديثات school-web — 2026-09-06

## نطاق الدمج

- النسخة المحلية الأصلية: `db916795` على `codex/client-uat-integration-20260901`.
- نسخة الخادم المنشورة: `ee6bd590`، وتشمل التسكين الأسبوعي وإصلاحات المعلم وBBB.
- فرع الحضور والاعتذار والتأجيل: `codex/attendance-apology-automation-20260906` عند `1afaac7a`.
- مصدر العمل: `/home/gamer/e-school` داخل Ubuntu/WSL2. لم تُعدّل نسخة Windows القديمة.
- فرع المراجعة: `codex/server-updates-integration-20260906`؛ الهدف فرع التكامل الحالي، وليس `main`.

## التعارضات والتصحيحات

1. تاريخ التسكين المحلي وتاريخ الخادم مختلفان لأن تحديثات التسكين طُبقت سابقًا كـpatch.
   دُمج التاريخان بـmerge عادي يحفظ كل commits؛ لم يُستخدم reset أو force push.
2. التعارض النصي الوحيد بين المحلي ونسخة الخادم كان مقدمة `docs/PROGRESS.md`.
   حُل بالاحتفاظ بسجل رحلة المعلم وبكل سجل التسكين المحلي. بعد الحل تطابقت شجرة
   الدمج مع شجرة `ee6bd590` تمامًا، قبل إضافة فرع الحضور.
3. دمج الحضور فوق هذه النسخة لم ينتج تعارضًا نصيًا. حُفظت مفاتيح التسكين الأسبوعي
   ومهلة الاعتذار/التأجيل الجديدة، واختبارات الخانات الأسبوعية والتأجيل معًا.
4. كشفت الجولة الشاملة الأولى ثلاثة إخفاقات فعلية: أُضيف `excusedAt` إلزاميًا إلى
   `SessionParticipantAdministrationData` فانكسر مستهلكو Attendance وAcademicReports
   وReporting. نُقل الحقل الجديد إلى نهاية التوقيع بقيمة افتراضية `null`؛ مستدعي
   الإنتاج يواصل تمرير قيمة العذر صراحة، والاختبارات القديمة بقيت دون تغيير.
5. استُبدلت قسمة راتب العقد الشهري باستخدام float بقسمة صحيحة وباقي قسمة، مع
   الحفاظ على التقريب half-up. سبع حالات تغطي القسمة الدقيقة ونصف الوحدة والقيم
   الكبيرة؛ أثبت الاختبار أن التنفيذ السابق يحوّل أكبر bigint موجب إلى قيمة سالبة.
   أُزيل مفتاح rounding غير المستعمل بدل إيهام المشغل بأنه يغير السياسة.
6. صُحح إعداد fixture في اختبار إعادة إنشاء غرفة BBB الجديد ليكون نوعه مفهومًا
   للمحلل الساكن، دون إضافة ignores أو إخفاء أخطاء.
7. حُدّثت `docs/13-scheduling-rules.md` لتطابق مهلة الساعة ودورة الاعتذار والتأجيل
   الحالية، مع حفظ شرح التسكين الأسبوعي.

## التحقق

- PostgreSQL 17 مؤقتة على شبكة Docker خاصة، دون ports، وبياناتها في tmpfs.
- حاوية PHP مؤقتة مرتبطة بنسخة WSL2 مع تطابق checksum لملف إعداد متغير.
- جميع اختبارات Pest عبر `scripts/test-isolated.php`: قاعدة وstorage مستقلان لكل تشغيل.
- الجولة الأولى: 1229 ناجحًا، 3 إخفاقات توافق DTO، و7549 تأكيدًا.
- الاختبار المالي قبل الإصلاح: 6 ناجحة وواحد فاشل يثبت فقدان الدقة/overflow.
- الحزمة الموجهة بعد التصحيح: 41 اختبارًا ناجحًا، 206 تأكيدات، وتشمل الهجرات
  على بيانات اصطناعية قائمة، down، وإعادة التطبيق.
- Pint العام: 2798 ملفًا ناجحًا؛ أعيد Pint للملفات الخمسة المصححة ونجح.
- TypeScript وVite build: ناجحان.
- PHPStan النهائي: 91 ملف مصدر متغير بلا أخطاء؛ ملف اختبار الحساب المالي الجديد
  حُلّل منفصلًا وبلا أخطاء كذلك. الفحص العام انخفض من 855 إلى 848 خطأ؛ أُزيلت
  أخطاء DTO الثلاثة وأخطاء typing الأربعة دون خطأ بديل أو ignore جديد.
  نسبة الأخطاء المتبقية إلى المصدر السابق مبنية على مقارنة الملفات والسطور مع
  `db916795`؛ لم يُعد تشغيل الفحص العام كاملًا على checkout قديم.
- الجولة الشاملة النهائية: **1239 اختبارًا ناجحًا و7613 تأكيدًا** خلال 245.84 ثانية،
  وتشمل Unit وFeature وArchitecture، دون إخفاقات.

## حدود النتيجة وحالة النشر

هذا دمج للكود، وليس نشرًا على الإنتاج. لم تُشغّل هجرات أو عمليات نشر أو اختبارات
على قاعدة الإنتاج. بقيت ملفات تشغيل الخادم غير المتتبعة في مكانها دون تغيير.
لا تعني النتائج إغلاق بوابة جاهزية الإنتاج: أخطاء PHPStan العامة السابقة قائمة،
ولم تُقَس التغطية أو تُنفّذ جولة متصفح جديدة في هذه المراجعة. اختبار حفظ ساعة
التسكين في المتصفح مثبت في مهمة المصدر التي نشرت `ee6bd590`.

سكربت `/root/51-deploy-code.sh` ما زال يحتوي `git reset --hard`؛ فُحص للقراءة فقط
ولم يُشغّل أو يُعدّل. النشر التالي يحتاج فحص رأس الخادم مجددًا والحفاظ على أي
commits جديدة، ولا يجوز افتراض أن نجاح هذا الدمج يجعل أي نشر لاحق آمنًا تلقائيًا.
