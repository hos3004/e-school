<?php

declare(strict_types=1);

return [

    /*
    |--------------------------------------------------------------------------
    | الصلاحيات الاختيارية — تُمنح للحساب مباشرة لا عبر دوره
    |--------------------------------------------------------------------------
    |
    | صلاحيات حساسة تبقى خارج حزم الأدوار عمدًا، ويقررها المسؤول لكل حساب على
    | حدة وقت إنشائه أو بعده. تظهر هذه الأسماء كمفاتيح في شاشة المستخدم لمن
    | يملك accesscontrol.permissions.grant_direct.
    |
    | مثالها الأول دور supervisor: الدور نفسه قراءة بلا مال ولا بيانات تواصل
    | ولا تسجيلات، وكل مشرف يُمنح منها ما يحتاجه فقط.
    |
    | أي اسم هنا يجب أن يكون صلاحية موجودة في مصفوفة docs/06-permissions-matrix.
    |
    */

    'optional_direct_permissions' => [
        'payroll.view',
        'contact.pii.view',
        'recording.view',
    ],

];
