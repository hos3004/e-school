# خريطة مشروع e-school

> آخر تحديث: 2026-08-31. هذه الخريطة تشغيلية مختصرة؛ العقود التفصيلية تحت `docs/`.
> قاعدة البيانات الحالية وبياناتها تجريبية بالكامل، لكن ضوابط الصلاحيات والمال
> والتكاملات تُعامل كضوابط إنتاج لأن الكود نفسه مرشح للنشر لاحقًا.

## [TECH_STACK]

- PHP 8.4 داخل Docker، Laravel 13.26.1، Filament 5.7.6، Livewire 4.4.1.
- React 19.2.8، TypeScript 5.9.3، Vite 7.3.6، PostgreSQL، Redis.
- تصدير التقارير PDF محليًا عبر mPDF 8.3.1 بخط DejaVu Sans ودعم RTL، دون خدمة
  متصفح خارجية أو تعطيل sandbox.
- لم يضف نظام نماذج التسجيل أي تبعية جديدة؛ يستخدم Repeater/HasMany المتاح في Filament.

## [SYSTEM_FLOW]

`RegistrationForm slug` منشور → صفحة Inertia عربية RTL → تحقق FormRequest من الحقول
الأساسية ومخطط أسئلة النموذج → `SubmitPublicRegistrationFormAction` → إنشاء مسودة
`RegistrationApplication` تحمل `registration_form_id` ولقطة الإجابات → انتقال رسمي
إلى `submitted` وحدث المجال → شاشة تسجيل الطلاب مع عمود وفلتر مصدر النموذج → مراجعة
وقبول الإدارة؛ لا ينشأ `StudentProfile` قبل القبول.

إدارة النموذج: مسؤول يملك `student.create` → نموذج Filament واحد → إضافة/حذف/نسخ/
ترتيب أسئلة HasMany → حفظ السبب والقيم قبل/بعد في `audit_log` → نشر رابط slug مستقل.

التقارير التشغيلية: مستخدم يملك `report.view` → يختار اليوم/الأمس/الأسبوع الحالي/
السابق/الشهر أو تاريخين → `OperationalReportCriteriaFactory` يحوّل حدود يوم المستخدم
إلى فترة UTC نصف مفتوحة → `OperationalReportQueryService` يجمع حصص Sessions
والمشاركين والحضور وأسماء الطلاب والمعلمين والمجموعات والمقررات وحالة التقرير عبر
Public Contracts وDTOs مجمعة → ملخص وبنود قابلة للبحث والفرز والترقيم. عند التصدير
يلزم `report.export` وتُعاد نفس الفلاتر إلى قالب عربي/إنجليزي/فرنسي ثم mPDF، مع
`no-store` وتنظيف مجلد العمل المؤقت في جميع الحالات.

## [ARCHITECTURE]

- `registration_forms` و`registration_questions` و`registration_applications` ملك
  موديول Students؛ لا join أو Model عابر لحدود الموديولات.
- النموذج هو مصدر الطلب الثابت، بينما سؤال «من أين سمعت؟» يبقى مصدرًا تسويقيًا
  اختياريًا داخل لقطة الإجابات. تعديل السؤال لاحقًا لا يغيّر الطلبات القديمة.
- الحقول الأساسية للهوية والجغرافيا ثابتة لتوافق دورة القبول الحالية؛ مرونة المنشئ
  مخصصة لأسئلة البرنامج: نص، نص طويل، رقم، قائمة، اختيار فردي، واختيارات متعددة.
- إلغاء النموذج يتم بالتعطيل لا الحذف، والأسئلة المحذوفة تستخدم SoftDeletes.
- Reporting لا يستورد Models ولا يعرف جداول الموديولات الأخرى؛ القراءة المجمعة تمر
  عبر عقود Sessions وAttendance وStudents وStaff وGroups وAcademics وAcademicReports.
  التنفيذ دفعي لتجنب N+1، وعزل المؤسسة مشتق من المستخدم الموثق لا من مدخل العميل.
- صفحة `admin/operational-reports` هي واجهة التقارير اليومية/الأسبوعية/المخصصة،
  وتقرير الطالب أو المعلم أو المجموعة هو نفس Read Model مع فلتر إلزامي مناسب؛ بذلك
  تبقى قواعد الملخص والتصدير موحدة بدل إنشاء مسارات متكررة لكل نوع.

## [ORPHANS & PENDING]

### تنقّل لوحة الإدارة — مغلق 2026-09-02

أُعيد تنظيم الشريط الجانبي إلى **خمسة أقسام** بمستويين
(`App\Filament\AdminNavigation`). كان يعلن عشر مجموعات ويعرض أربعًا وعشرين، لأن
تسميات الموارد تأتي من ملفات ترجمة الموديولات ولم تطابق أيًّا من المعلنة. يحرس
البنيةَ الآن `tests/Feature/AdminNavigationStructureTest.php`. لا معلّقات في هذا
النطاق: 91 اختبارًا/2681 توكيدًا تشمل `AdminPanelSmokeTest` و`tests/Architecture`.

### نواقص رحلة العميل الثمانية — مفتوحة

التفاصيل والأدلة في `docs/uat/2026-09-02-journey-readiness.md`.

- **أُغلق نقص الحضور الآلي في 2026-09-06.** أحداث
  `ClassroomParticipantJoined/Left` تجمع فترات الاتصال داخل الموعد الرسمي،
  و`ClassroomEnded` ينشئ كشفًا أوليًا وفق 75%/40%. دخول ما بعد النهاية لا
  يُحتسب رغم بقاء الرابط 15 دقيقة. المعلم المعيّن يعتمد الكشف أو يتجاوزه بسبب
  بعد إثبات حضوره من أحداث الغرفة، والغياب المؤكد منه وحده يدخل عدّاد 30 يومًا
  ويجمّد القيد آليًا عند الحد الثالث.
  اعتذار المعلم يطلق بحث بديل فوريًا ودوريًا حتى تغطية الحصة.
- **قناة Push (FCM) معلنة بلا `gateway`** في `config/notifications.php`، وافتراضها
  معطّل. إشعارات الجدولة تصل عبر داخل التطبيق والبريد وواتساب (24 رسالة مثبتة).
- **لا زر جدولة من داخل المجموعة** — `ViewGroup` فيه إسناد المعلم ووضع الطالب
  وربط البرنامج فقط، فينقطع التسلسل عند تحديد المواعيد.
- **22 موردًا بلا زر إجراء ولا حالة فارغة موجِّهة** (`canCreate(): false`)،
  و`emptyStateHeading` مستخدم في ملف واحد بالمستودع كله.
- **`docker/php/php.ini` يثبّت `memory_limit = 512M`** فتسقط الجولة الكاملة عند
  تحليل AST للحدود المعمارية. برفعه تكتمل في 41 ثانية.

- لا توجد أجزاء كود أو ربط معلقة في نظام نماذج التسجيل. نجحت 34 اختبارات موجهة
  بـ90 توكيدًا، واختبار ملكية الجداول 3/3، وrollback/reapply للهجرة، وPint وPHPStan
  الموجهان، وTypeScript وبناء Vite.
- QA البصري التفاعلي RTL/LTR فقط لم يُنفذ: Docker Desktop يعمل لكن bind mount
  للخدمة الجارية يرى نسخة WSL قديمة؛ تحقق PHP تم داخل حاوية مؤقتة بلا أسرار.
- لا توجد أجزاء كود أو ربط معلقة ضمن نطاق التقارير التشغيلية الإدارية: نجحت الحزمة
  الموجهة في 30 اختبارًا/158 توكيدًا، ومنها توليد PDF عربي حقيقي، وعزل المؤسسة،
  صلاحيات الطالب/المعلم، والفلاتر الزمنية عبر DST، ومسح cursor لكل صفحات المصدر
  قبل تطبيق سقف النتائج حتى لا تُفقد المطابقات المتأخرة. نجح Pint وPHPStan
  بلا أخطاء، وComposer validate/audit، وملكية الجداول وحدود Reporting/Sessions/
  AcademicReports والعقود العامة والأنواع الصارمة ونظافة الكود. تمكين قائمة
  AcademicReports القديمة مباشرة في بوابة الطالب/ولي الأمر نطاق منفصل؛ تقرير الطالب
  المطلوب هنا متاح للإدارة بفلتر الطالب الكامل ولا يعتمد على ذلك المسار القديم.

## مدخل التنفيذ الحالي

- مصدر نطاق المرحلة الأولى الوحيد: `docs/phase-1-approved-scope.md`.
- طابور Antigravity الوحيد: `docs/agent-tasks/QUEUE-antigravity.md`، وفيه أربع مهام
  متتابعة تُنفّذ كل واحدة في محادثة جديدة.
- ملفات `AGENT-A`…`AGENT-H` و`docs/20-agent-task-packages.md` تاريخية وليست أوامر حالية.
- المرحلة الأولى تشمل التشغيل الأكاديمي وBBB والتسجيلات والحضور والتجميد والتقارير
  والتواصل والتكليفات؛ المالية وZoom خارجها.

## صورة النظام

- تطبيق Laravel واحد بنمط **Modular Monolith**، وواجهة Filament للإدارة وInertia/React
  لبوابات الطالب والمعلم وولي الأمر.
- PostgreSQL هو مخزن البيانات، وRedis للطوابير/الكاش، والتشغيل المحلي عبر Docker Compose.
- نقطة اكتشاف الموديولات: `shared/src/Module/ModuleRegistry.php`، وترتيبها في
  `config/modules.php`. الحدود الآلية في `tests/Architecture/`.
- هوية المستخدم الفعلية: `Modules\Identity\Domain\Models\User`؛ لا يوجد
  `App\Models\User`.

## طبقات الموديولات واتجاه الاعتماد

1. النواة: Organization, Identity, AccessControl, Audit, Integrations, Notifications.
2. الأشخاص: Students, Guardians, Staff.
3. الأكاديمي: Academics, Groups, Enrollments, Content.
4. التشغيل: Scheduling, Sessions, Attendance, VirtualClassroom, Recordings.
5. التعلّم: Assignments, Assessments, AcademicReports, Certificates.
6. الانضباط والتواصل: Discipline, Messaging.
7. المال: Payroll وBilling موجودان معماريًا لكنهما مؤجلان بالكامل خارج قبول المرحلة الأولى.
8. القراءة: Reporting.

التواصل العابر للموديولات يكون بأحداث المجال أو العقود العامة أو Query Services
تعيد DTOs. يمنع استيراد نماذج Eloquent أو الربط بين جداول موديولين في كود الموديول.

## التدفقات الحساسة

### الدخول والصلاحيات

`web auth` → مستخدم Identity → Laravel Gate →
`AccessControlQuerier::modelHasPermission()` → الأدوار/المنح المباشرة في PostgreSQL.
مسارات البوابات خلف `auth` و`can:`، ولا توجد تجاوزات بأسماء الأدوار. بيانات العرض
تسند أدوار student/teacher/guardian إلى حسابات الديمو عبر البذرة الجذرية.

### بوابات المستخدم

`routes/web.php` → متحكمات `app/Http/Controllers/Portal/` → خدمة قراءة DB فقط →
صفحات `resources/js/Pages/{Student,Teacher,Guardian}`. يشارك
`HandleInertiaRequests` بيانات المستخدم واللغة والقواميس. نطاق ولي الأمر يخفي الطالب
غير المرتبط بإرجاع 404، ونافذة دخول الحصة مصدرها `config/virtual-classroom.php`.

### التسجيل والقبول والجغرافيا

`Identity User` (username + بريد أو هاتف) → `RegistrationApplication` → تقديم ومراجعة
→ قبول ينشئ `StudentProfile` داخل معاملة واحدة → `waiting_assignment` →
`Enrollments` يستدعي `StudentAdmissionQueries` قبل أي توزيع. التسجيل لا يعني قبولًا،
والقبول لا يعني توزيعًا.

إنشاء الطالب من لوحة الإدارة يدعم حسابًا جديدًا بالكامل أو حسابًا موجودًا من
المؤسسة نفسها. `CreateStudentOnboardingAction` ينسّق الحساب والطلب والقبول والملف
وإسناد الدور داخل معاملة واحدة، مع سبب وتدقيق. التسكين من قائمة الطلاب أو ملف
الطالب يمر حصريًا عبر `AssignStudentToGroupAction` بعد اختيار البرنامج ثم الكورس
ثم مجموعة متاحة بالسعة والمعلم المؤهل؛ لا تكتب واجهة Filament في جداول العضوية.

ملفا الطالب والمعلم في الإدارة هما Hubs مبنيان على
`ProfileAdministrationQueryService`: يجمع DTOs من عقود Identity وAcademics وGroups
وEnrollments وSessions وStaff، ولا يستورد نموذجًا أو جدولًا عابرًا للحدود.

الدول والمناطق مملوكة لـOrganization وتصل إلى بقية الموديولات عبر
`GeographyQueries` وDTOs فقط. بادئة اسم المستخدم تُقرأ عبر
`OrganizationSettingQueries`. تأهيل المعلم للكورس مملوك لـStaff ويُقرأ عبر
`TeacherQualificationQueries` لا عبر جدول أو نموذج عابر للحدود.

### الإشعارات

حدث/خدمة تطبيق → `NotificationDispatcher` → `notification_outbox` → أمر توزيع
المستحق → `SendQueuedNotification` → بوابة القناة من Integrations → سجل محاولات
التسليم. الصندوق هو حاجز الإرسال، مع idempotency، ساعات هدوء، backoff وحالات Enum.

### الفصل المباشر

`VirtualClassroomProvider` → `NullProvider` للاختبار أو
`BigBlueButtonProvider` للإنتاج → BBB API. الأسرار والمهل وإعادة المحاولة وقاطع
الدائرة من `config/virtual-classroom.php` وبيئة التشغيل. Webhook لا يُفسّر قبل
التحقق من توقيعه.

### المال — مؤجل خارج المرحلة الأولى

`payroll_entries` و`billing_entries` دفاتر append-only. التصحيح بقيد تسوية جديد؛
لا تعديل أو حذف لقيد إنتاجي، ولا حسابات مالية بـ float.

## البيانات والحماية

- كل التواريخ التشغيلية UTC وتُعرض بتوقيت المستخدم.
- بيانات البشر تستخدم SoftDeletes؛ التعليق/الأرشفة بدل الحذف.
- تغييرات الحضور والحالة الأكاديمية والمال والصلاحيات والتسجيلات يجب أن تدخل
  `audit_log` مع السبب والقيم قبل/بعد.
- لا أسرار في المستودع. مفاتيح BBB ومزوّدي الإشعار تأتي من `.env`.
- بيانات قاعدة التطوير الحالية وهمية ومسموح إعادة بذرها؛ هذا لا يخفف قواعد كود
  الإنتاج أو يسمح بجعل الحذف الصلب جزءًا من مسارات التطبيق.

## أوامر التحقق والتشغيل

كل أوامر PHP/Composer/Artisan داخل Docker:

> **مؤقتًا قبل إغلاق Task 01:** لا تشغّل `php artisan test` أو `composer check` الخام؛
> قاعدة الاختبار المشتركة تصادمت سابقًا. استخدم أمر البيئة المعزولة الذي سيوثقه
> `REPORT-ANTIGRAVITY-01.md`. الأوامر غير الكاتبة التالية فقط آمنة للجرد الحالي.

```bash
docker compose exec -T app php artisan route:list --json
docker compose exec -T app vendor/bin/pint --test
docker compose exec -T app vendor/bin/phpstan analyse --memory-limit=1G
```

## حالة الجاهزية والمخاطر المعروفة

- المصدر الحاكم الجديد لحالة لوحة الإدارة هو `docs/admin-panel-readiness.md`؛ يمنع
  استخدام عدد الملفات أو وجود Resource كدليل اكتمال. الحالة الوحيدة القابلة للعرض
  للعميل هي `CLIENT_READY` بعد الاختبارات والفحص البصري.
- شريحة Staff التشغيلية الحالية تنشئ المعلم وحسابه وعقده وسعره وتأهيلاته ودوره
  ذريًا، وتعرض Hub للحساب والعقد والمجموعات والإتاحة والحصص. قرار الإتاحة مدقق بسبب،
  وAPI محصور بالملكية والمؤسسة. ما زالت الاعتذارات/البدلاء/التوظيف والفحص البصري
  معلنة كنواقص، ولذلك Staff ليس `CLIENT_READY` بعد.
- شريحة Guardians تدعم حسابًا جديدًا أو موجودًا، ملف الوصي، الربط الأولي، Hub
  بأسماء الطلاب والحسابات، وتوثيق/تعيين الأساسي/فك الرابط بأسباب وتدقيق. أسماء
  الصلاحيات القديمة غير الموجودة أزيلت من المسارات الفعلية، والنطاق مؤسسي. روابط
  الوصاية تُؤرشف SoftDelete ولا تُحذف. الفحص البصري ما زال معلّقًا عند الدخول.
- Identity وAccessControl في حالة `VERIFIED`: إنشاء الحساب التشغيلي مع دوره وتدقيقه
  ذري، وHub الحساب يعرض الأدوار والأجهزة والحالة. كل تغيير في دورة الدور أو منح
  الصلاحية يتطلب سببًا ويسجّل قبل/بعد، ونجحت 40 اختبارًا/87 توكيدًا لحزمة التحكم
  بالوصول إضافة إلى 23 اختبارًا/65 توكيدًا لحزمة الحساب والإسناد. يبقى QA بصري فقط.
- Groups في حالة `VERIFIED`: إنشاء المجموعة لا يقبل مؤسسة من المستخدم، وHub المجموعة
  يجمع البرامج والمعلمين والطلاب والحصص عبر DTOs. الربط والإسناد والتسكين والحالات
  والأرشفة تمر عبر Actions بسبب وتدقيق وعزل مؤسسة؛ نجحت 48 اختبارًا/131 توكيدًا
  وPHPStan الموجّه. يبقى QA بصري وبيانات عرض فقط.
- Academics في حالة `VERIFIED`: Hubs البرامج والمستويات والكورسات تعرض المسار
  الأكاديمي والتصنيفات والأهلية وقواعد التقديم والتسليم. كل الكتابات تمر عبر Actions
  مؤسسية بسبب وتدقيق، ولا تقبل المؤسسة من الواجهة؛ نجحت 57 اختبارًا/146 توكيدًا،
  وPHPStan وPint الموجهان بلا أخطاء. يبقى QA بصري وبيانات عرض فقط.
- Enrollments لديه الآن مركز عمليات يعرض الاسم والبرنامج والمستوى والمجموعات وسجل
  الحالة، وينفذ الإنشاء والتسكين والتجميد/العودة وتغيير المستوى عبر Actions مدققة.
  التفعيل المباشر بعد القبول ممنوع؛ التسكين الآمن هو المسار الوحيد ويكتب history
  وaudit. نجحت 6 اختبارات/27 توكيدًا إضافة إلى قبول التسكين 2/61. يبقى ربط ملف
  العودة بـDiscipline ونقل المجموعة وQA البصري، لذلك الحالة `IN_PROGRESS`.
- Content في حالة `VERIFIED`: مكتبة المواد تربط الكورس داخل المؤسسة وتدعم مسودة/
  نشر، نافذة إتاحة، ترتيبًا وإصدارات immutable مع السبب والفاعل والتدقيق. API الإدارة
  لا يكشف disk/path. نجحت 6 اختبارات/36 توكيدًا وmigration up/down وPHPStan/Pint؛
  يبقى تسليم الملف للطالب بعد فحص القيد وQA البصري.
- Scheduling أصبح مركز تشغيل حقيقي بحالة `IN_PROGRESS`: إنشاء قالب أسبوعي لمجموعة
  أو طالب، اختيار كورس ومعلم مؤهلين من عقود الموديولات، حفظ RRULE، توليد حصص UTC
  ومشاركين حتى الأفق المحدد مع ثبات الساعة المحلية عبر DST، منع تعارض المعلم
  والمجموعة والطالب، وحماية الحصص الواقعة داخل 48 ساعة عند تعديل/إيقاف القالب.
  التعديل يستبدل الأحداث المستقبلية بحالة `Superseded` بلا أثر مالي، وكل عملية
  بسبب وتدقيق. التسكين الآمن يضيف الطالب تلقائيًا للحصص المستقبلية، واعتماد
  التأجيل يمر عبر `SessionSchedulingGateway` وينشئ التلافي ونسخ المشاركين داخل
  معاملة واحدة. نجحت 5 اختبارات/34 توكيدًا، PHPStan الموجّه، و87 اختبار حدود/
  2459 توكيدًا. المتبقي: job انتهاء SLA، قبول الموعد البديل في بوابة الطالب،
  تخطي العطل الفعلي، والفحص البصري. إبطال الدعوات عند خروج الطالب أُغلق في Sessions.
- Sessions أصبح مركز تشغيل حقيقي بحالة `IN_PROGRESS`: قائمة وتقويم بأسماء فعلية،
  وHub يجمع البرنامج والمقرر والمجموعة والتوقيت والمعلم الأصلي والمنفذ والطلاب
  والحضور الأولي والنهائي/المشتق وسجل الحالات والبدلاء والاعتذارات والإشعارات
  وAudit. لا تعديل مباشر للحالة؛
  كل انتقال بسبب وفاعل ومعاملة، ومورد المشاركين قراءة فقط ولا يكشف رموز الدخول.
  خروج الطالب يبطل دعوات المستقبل، وبوابة الطالب تحترم الإبطال، وإعادة التسكين
  تعيد التفعيل مع تدوير الرمز. المرشح البديل يقرأ التأهيل والنشاط والإجازات عبر
  عقود Staff الرسمية بدل استنتاجها من إسناد المجموعة. نجح تراجع التشغيل الموحد
  Sessions/Attendance/VirtualClassroom/Recordings في 98 اختبارًا/395 توكيدًا.
  المتبقي: إنشاء الحصة المنفردة ومعاينة التكرار ثم QA البصري RTL.
- Attendance في حالة `VERIFIED`: قائمة ومركز بأسماء تشغيلية فعلية، عزل مؤسسة على
  القائمة والسياسة والـ Actions، والتحقق من الدعوة الفعالة. الرصد والاعتماد والتجاوز
  بسبب مكتوب وتدقيق قبل/بعد، بلا CRUD أو حذف مباشر. عقدا القراءة العامان يُدمجان
  النتيجة داخل مشاركي مركز الحصة عبر منسق التطبيق. نجح الموديول 34 اختبارًا/108
  توكيدات، وتراجع مركز Sessions ‏4/35، وPHPStan/Pint وmigration down/up والحدود
  87/2459. المتبقي roster جماعي ومؤشرات وQA بصري RTL.
- VirtualClassroom في حالة `VERIFIED`: دورة حياة مستقلة للمزوّد تحفظ pending/
  provisioned/running/ended/failed، وعدد المحاولات والخطأ الأخير. يوجد تجهيز آلي
  للحصص كل دقيقة داخل نافذة 20 دقيقة، وإعادة محاولة وفحص صحة من H6 بسبب وتدقيق.
  webhook يطابق المستخدم المحلي ويحدّث running/ended ويكتب Audit، وH6 يعرض الحالة
  والصحة والأحداث دون أسرار الدخول. نجح 17 اختبارًا/81 توكيدًا، وتراجعات الدمج
  8/64، وPHPStan/Pint وmigration down/up والحدود 87/2459. المتبقي smoke test حي
  على خادم BBB الحقيقي وQA بصري RTL.
- Recordings في حالة `VERIFIED`: القائمة ومركز التفاصيل يعرضان سياق الحصة والكورس
  والمعلم والحالة والمدة والمنح والمشاهدات بأسماء فعلية، بلا مسار تخزين أو معرّفات
  خام. منح المستخدم/المجموعة وإلغاؤها معزولان بالمؤسسة وبسبب وAudit، والانتهاء لا
  يتجاوز عمر التسجيل. التشغيل يتطلب رابطًا موقّعًا مؤقتًا ويمر بقرار وصول يجمع
  صلاحية المشرف أو معلم الحصة أو منحة طالب ذي قيد نشط، وكل مشاهدة append-only.
  أمر `recordings:enforce-retention` يعمل كل ساعة ويكمل archived إلى expired بدل
  تركه يتيمًا. نجح الموديول 29 اختبارًا/125 توكيدًا، والتراجع التشغيلي 41/216،
  وPHPStan/Pint والحدود 87/2459. المتبقي QA بصري
  واختبار التخزين/المزوّد الفعلي فقط.
- Notifications في حالة `VERIFIED`: جرس ومركز إشعارات مشترك للبوابات مع حالات
  التحميل/الخطأ/الفراغ، قراءة مفردة/جماعية وروابط داخلية آمنة حسب الصلاحية. مركز
  الإدارة يعرض المستلم الفعلي والمحتوى المترجم والمحاولات والأخطاء، وإعادة المحاولة
  أو الإلغاء يدويًا لا يتمان دون سبب وAudit. دُمج تاريخ إشعارات الحصة داخل H6،
  وأُصلحت مصادقة Sanctum للـSPA فعليًا. نجح الموديول 103 اختبارات/523 توكيدًا،
  ونجح TypeScript والبناء، كما اجتازت صفحة المعلم العربية RTL اختبار المتصفح الحقيقي.
  المتبقي تفضيلات المستخدم وفحص صفحات الإدارة بصريًا؛ إعداد ESLint العام مفقود من
  المستودع ويظل نقص tooling موثقًا لا فشلًا مخفيًا.
- Assignments في حالة `CLIENT_READY`: تحولت القائمة والإنشاء والعرض من CRUD سطحي
  إلى رحلة برنامج ← مقرر ← مجموعة ← معلم مؤهل، ثم إنشاء roster الطلاب النشطين
  ذريًا. Hub الواجب يعرض المؤشرات والتسليمات الفعلية وسجل التدقيق، والتصحيح يحفظ
  الدرجة الأصلية والخصم والنهائية. اجتازت الحزمة 10 اختبارات/58 توكيدًا، وتكامل
  الإشعارات والبوابة 26/120، وPHPStan/Pint وmigration up/down. أُنشئ واجب عرض
  حقيقي لطالبين وفُحصت القائمة والنموذج المتسلسل والHub بالعربية RTL بلا أخطاء
  Console؛ لذلك هذه أول شريحة تعلم قابلة للعرض للعميل فعليًا.

- نواة حزمة التسجيل والجغرافيا A منفّذة: البيانات المرجعية، طلبات القبول، إنشاء ملف
  الطالب عند القبول، أسماء المستخدمين، جغرافيا الطالب/المعلم وتأهيل المعلم للكورس.
  تقرير الإثبات والنتائج في `docs/agent-tasks/REPORT-A.md`.
- رحلة A ليست مكتملة إنتاجيًا بعد: دخول Filament لا يقبل username، واستعادة الحساب
  عبر الهاتف/WhatsApp غير منفّذة، وإشعار التقديم يحتاج resolver لمستلمي
  الإدارة/ولي الأمر. قبول الطالب الإداري والتسكين وإسناد دوره أصبحت مدققة بأسباب.
- فلاتر A موجودة، لكن البحث الموحد بأسماء الحسابات القديمة يحتاج Query read-only من
  Identity. اختبار التوزيع الكامل محجوب بfixture خارج A في بيانات البرنامج.
- اعتماد Identity على Organization فُصل بمنفذ يملكه Identity وAdapter في طبقة
  تركيب التطبيق؛ اختبارات أسماء المستخدمين ما زالت خضراء، وإخفاق الحدود الخاص به زال.
- العمل الجاري الآخر يغطي: الترجمات الناقصة، توحيد صلاحيات موارد Filament، بوابات
  Inertia، محرّك الإشعارات، وموفّر BigBlueButton.
- لا يعتمد هذا التوزيع القديم كطابور. التنفيذ الجديد مقسّم إلى المهام الأربع في
  `docs/agent-tasks/QUEUE-antigravity.md`، ولا تُقبل حزمة من تقرير المنفّذ وحده.
- توجد عقود كتابة قديمة في بعض صفحات المعلم لا تطابق APIs الحالية، ولا يوجد بعد
  مسار `/locale` أو مسارات HTTP لاعتماد/اقتراح التأجيل؛ لا ينبغي اختراع URLs وهمية.
- بعض فحوص `can()` القديمة خارج موارد Filament ما زالت تستخدم أسماء غير موجودة في
  مصفوفة الصلاحيات وتحتاج موجة توحيد منفصلة مع Policies واختبارات.
- قائمة تقارير الحصص في AcademicReports محصورة الآن بمعرّفات حصص المؤسسة عبر عقد
  Sessions، والمعلم الذي لا يملك `staff.view.any` محصور بملفه عبر عقد Staff. يبقى
  نطاقا الطالب `own` وولي الأمر `children` بحاجة إلى عقود ملكية عامة قبل تمكين هذه
  القائمة لهما؛ عزل المؤسسة هو الحد الآمن الحالي ولا يُعد اكتمالًا لهذين النطاقين.
- التحقق الموجّه لرحلات الإدارة المنجزة ناجح، ونجحت حزمة Identity وAccessControl
  الأخيرة في 123 اختبارًا/359 توكيدًا. PHPStan الموجّه بلا أخطاء.
- آخر جولة كاملة مستقلة: **686 ناجحًا و75 فاشلًا و4502 توكيدًا**. التحقق
  الموجّه لملفات الإنتاج المتأثرة خرج بصفر أخطاء PHPStan، لكن PHPStan العام تجاوز
  1000 خطأ. اختبارات الحدود المعمارية الحالية خضراء بالكامل: 87 اختبارًا/2459 توكيدًا.
- لا تُصنّف النسخة جاهزة للإنتاج قبل نجاح الفحص الشامل، اختبار الهجرات على قاعدة
  نظيفة ومعبأة مع rollback، وتجربة رحلة البوابات وBBB يدويًا بإعدادات حقيقية.

## صور المستخدمين (Avatar) ودليل المعلمين — 2026-08-25

- **Avatar موحّد:** المصدر الوحيد `users.avatar_path` (لا حقول في ملفات الطلاب أو
  الموظفين، ولا Base64). الإعداد المركزي في `config/avatars.php` (قرص، مجلد، حد
  الحجم، MIME المسموح JPEG/PNG/WebP، الصور الافتراضية).
  - `Modules\Identity\Domain\Contracts\AvatarQueries` + `AvatarResolver`: يعيد
    رابط الصورة المرفوعة إن كان الملف صالحًا وإلا صورة افتراضية حسب الجنس
    (`public/images/avatars/default-{male,female,neutral}.png`). الجنس يُمرَّر من
    طبقة الاستدعاء (ملف الطالب/الموظف) دون استيراد Eloquent عابر.
  - `UserAccountOperations::setAvatar()` (تنفيذها في `UserAccountOperationService`):
    فحص محتوى الصورة على الخادم (magic numbers)، نقل من المجلد المؤقت إلى
    `avatars/{org}/{user}/{ulid}.{ext}` باسم عشوائي، حذف القديمة بعد نجاح الحفظ فقط،
    وسبب إداري إلزامي مع تدقيق `identity.avatar_updated`.
  - إجراء الرفع الموحّد: `Shared\Filament\UserAvatarAction` مستخدم في صفحات
    عرض/تعديل المستخدم والطالب والمعلم/الموظف؛ العرض الدائري مع صورة افتراضية
    حسب الجنس في قوائم وصفحات المستخدمين والطلاب والموظفين.
  - اختبارات: `modules/Identity/tests/Feature/AvatarManagementTest.php` (5).

- **دليل المعلمين التشغيلي (`admin/teachers-directory`):** صفحة Filament
  `Modules\Staff\Presentation\Filament\Pages\TeachersDirectory` فوق StaffProfile
  نفسه — لا جدول teachers. المعلم = حساب نشط + ملف موظف داخل المؤسسة. الضغط يفتح
  مركز عمليات المعلم القائم (`ViewStaffProfile`) وكل العمليات تبقى فيه.
  - قراءة مجمّعة بلا N+1 عبر عقود موسّعة: `TeacherDirectoryQueries`
    (جديد في Staff)، `GroupAdministrationQueries::activeAssignmentCountsForTeachers`
    و`SessionAdministrationQueries::countsForTeachers` (دفعة واحدة لكل صفحة)،
    و`UserQueryService::searchUserIdsForOrganization` للبحث بالاسم/البريد/الهاتف
    دون join عابر للموديولات.
  - الأعمدة: الصورة، الاسم، الكود الوظيفي، حالة الحساب، نوع التوظيف، الكورسات
    المؤهل لها، المجموعات النشطة، القادم/المكتمل/الملغى هذا الشهر، التوافر.
    فلاتر: النشاط، نوع التوظيف، الكورس، المجموعة، التوافر. الوصول بـ`staff.view.any`.
  - اختبارات: `modules/Staff/tests/Feature/TeacherDirectoryTest.php` (3)،
    وحدود المعمارية خضراء (87/2459) بعد التغييرات.

## مراجع القرار

- `AGENTS.md`: عقد العمل داخل المستودع.
- `docs/phase-1-approved-scope.md`: مصدر حقيقة نطاق المرحلة الأولى.
- `docs/agent-tasks/QUEUE-antigravity.md`: ترتيب التنفيذ الحالي.
- `docs/06-permissions-matrix.md`: مصفوفة الصلاحيات.
- `docs/08-module-boundaries.md` و`docs/09-domain-events.md`: الحدود والتواصل.
- `docs/12-notification-architecture.md`: تدفق الإشعارات.
- `docs/14-payroll-rules.md`: سلامة دفتر المستحقات.
- `docs/15-security-model.md`: نموذج الأمان.
- `docs/21-definition-of-done.md`: بوابة الإنجاز.

## تكامل واجهة وتجارب العميل — 2026-09-01

- فرع التكامل المعزول `codex/client-uat-integration-20260901` مبني على المرشح
  الوظيفي `5f282326`، وأُدخلت عليه commits التصميم الخمسة بالترتيب مع الحفاظ على
  routes والصلاحيات وعزل المؤسسة من الخط الوظيفي.
- أُغلقت الرحلات ذات الأولوية: تقرير الحصة للمعلم الأصلي/البديل، طلبات التأجيل
  للطالب والمعلم ومراجعتها إداريًا، إنشاء Popup Campaign، إرسال إشعار يدوي لطالب
  أو معلم أو مجموعة، الرسائل المشتركة، إنشاء التقرير الشهري، وتشغيل التقرير
  التشغيلي صراحة بعد اختيار الفلاتر.
- مصفوفة العمل والأدلة الحالية في `docs/uat/page-action-matrix.md`. الاختبارات
  المستهدفة والبناء والتحليل الساكن للملفات المعدلة ناجحة، لكن دورة Laravel الكاملة
  اصطدمت بحد ذاكرة PHP 512MB، ولم تكتمل جولة المتصفح الشاملة؛ الحكم الحالي ليس
  جاهزًا لـUAT أوالإنتاج حتى إغلاق هاتين البوابتين.
